"""
Plugin authoring templates — not platforms.

This package holds the `BaseDevice` example that new NOS plugins subclass.
It lives outside the `platforms_py/*.py` registry glob on purpose, so its
modules never surface as platforms (#239, G1 follow-up).
"""
